import styled from 'styled-components'
import tw from 'twin.macro'

export const HeaderContainer = styled.header`
    ${tw`
        w-full h-16 p-3 flex justify-between
    `};
`

export const Nav = styled.nav``
